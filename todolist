[x] find out why the images aren't showing
[x] add "a new issue" button
[] pop up base for new issue
- (not a priority) create a workflow for this project:
	- webpack for the js
	- scss to sass
	- run tests (create a simple test case for this)
- create a pop up that asks for cookies and their acceptance
- store data as json for todo
- save in local storage


CURRENT WORKFLOW COMMANDS:
npx webpack
live-server	// in dist folder to run the page
npm run scss	// to compile scss to css
npm run test


git:
git add .
git commit -m "your message"
git push -u origin main
